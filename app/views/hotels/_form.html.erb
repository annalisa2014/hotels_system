<%= form_for(@hotel) do |form| %>

  <% if @hotel.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@hotel.errors.count, "error") %> prohibited
        this hotel from being saved:
      </h2>
      <ul>
        <% @hotel.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>

  <p>
    <%= form.label :description %><br>
    <%= form.text_area :description %>
  </p>

  <p>
    <%= form.label :country_code %><br>
    <%= form.text_field :country_code %>
  </p>

  <p>
    <%= form.label :average_price %><br>
    <%= form.text_field :average_price %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
